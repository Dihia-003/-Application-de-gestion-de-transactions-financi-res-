<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mot de passe oublié - FinanceTracker</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root { --primary:#8B5CF6; --primary-strong:#7C3AED; --ring:rgba(139,92,246,.12); --text:#0f172a; --muted:#64748b; }
        * { box-sizing: border-box; }
        html, body { height: 100%; }
        body { margin:0; font-family:'Inter',sans-serif; color:var(--text); background:
            radial-gradient(1200px 600px at 10% 10%, rgba(255,255,255,.18), transparent),
            radial-gradient(1000px 500px at 90% 30%, rgba(255,255,255,.12), transparent),
            linear-gradient(135deg, #8B5CF6, #A78BFA); display:grid; place-items:center; padding: clamp(1rem, 3vw, 2rem); }
        .modal { width:100%; max-width: 480px; background: rgba(255,255,255,.28); border:1px solid rgba(255,255,255,.35); border-radius: 18px; box-shadow: 0 30px 60px -15px rgba(15,23,42,.35); padding:clamp(1rem,3vw,1.5rem); }
        .card { background:#fff; border-radius:14px; border:1px solid #eef2ff; box-shadow: 0 10px 30px rgba(2,6,23,.08); padding: clamp(1rem, 3vw, 1.75rem); }
        .title { text-align:center; font-weight:800; font-size: clamp(1.25rem,2.4vw,1.6rem); margin:0 0 .5rem; }
        .subtitle { text-align:center; color:#64748b; margin-bottom:1.5rem; }
        .form-group { margin-bottom:1rem; }
        label { display:block; margin:0 0 .4rem; font-weight:600; color:#334155; }
        input { width:100%; padding:.9rem 1rem; border:2px solid #e2e8f0; border-radius:12px; font-size:1rem; }
        input:focus { outline:none; border-color: var(--primary); box-shadow: 0 0 0 3px var(--ring); }
        .submit { width:100%; padding:1rem; border:none; border-radius:999px; background:var(--primary); color:#fff; font-weight:800; cursor:pointer; box-shadow: 0 10px 18px rgba(139,92,246,.35); }
        .submit:hover { background:var(--primary-strong); }
        .links { margin-top:1rem; text-align:center; }
        .links a { color:var(--primary); text-decoration:none; font-weight:700; }
    </style>
</head>
<body>
    <div class="modal">
        <div class="card">
            <h1 class="title">Mot de passe oublié</h1>
            <div class="subtitle">Entrez votre email pour recevoir un lien de réinitialisation</div>
            
            {{ form_start(requestForm) }}
                <div class="form-group">
                    {{ form_label(requestForm.email) }}
                    {{ form_widget(requestForm.email) }}
                </div>
                <button type="submit" class="submit">Envoyer le lien de réinitialisation</button>
            {{ form_end(requestForm) }}
            
            <div class="links">
                <a href="{{ path('app_login') }}">← Retour à la connexion</a>
            </div>
        </div>
    </div>
</body>
</html> 