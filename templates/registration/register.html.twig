<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inscription - FinanceTracker</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root { --primary:#8B5CF6; --primary-strong:#7C3AED; --ring:rgba(139,92,246,.12); --text:#0f172a; --muted:#475569; }
        * { box-sizing: border-box; }
        html, body { height: 100%; }
        body { margin:0; font-family:'Inter',sans-serif; color:var(--text); background:
            radial-gradient(1200px 600px at 10% 10%, rgba(255,255,255,.18), transparent),
            radial-gradient(1000px 500px at 90% 30%, rgba(255,255,255,.12), transparent),
            linear-gradient(135deg, #8B5CF6, #A78BFA); display:grid; place-items:center; padding: clamp(1rem, 3vw, 2rem); }
        .modal { width:100%; max-width: 820px; background: rgba(255,255,255,.28); border:1px solid rgba(255,255,255,.35); border-radius: 18px; box-shadow: 0 30px 60px -15px rgba(15,23,42,.35); padding:clamp(1rem,3vw,1.5rem); }
        .card { background:#fff; border-radius:14px; border:1px solid #eef2ff; box-shadow: 0 10px 30px rgba(2,6,23,.08); padding: clamp(1rem, 3vw, 1.75rem); }
        .title { text-align:center; font-weight:800; font-size: clamp(1.25rem,2.4vw,1.6rem); margin:0 0 .25rem; }
        .subtitle { text-align:center; color:#64748b; margin-bottom:1rem; }
        .google-btn { display:flex; align-items:center; justify-content:center; gap:.6rem; width:100%; background:#fff; border:1px solid #e2e8f0; border-radius:999px; padding:.8rem 1rem; font-weight:700; color:#334155; box-shadow: 0 6px 14px rgba(2,6,23,.06); cursor:pointer; text-decoration:none; }
        .google-btn:hover { box-shadow: 0 10px 18px rgba(2,6,23,.08); }
        .divider { display:flex; align-items:center; gap:.75rem; color:#94a3b8; font-weight:600; margin:1rem 0; }
        .divider::before, .divider::after { content:""; height:1px; background:#e2e8f0; flex:1; }
        .grid { display:grid; grid-template-columns: repeat(2, minmax(0,1fr)); gap:.9rem; }
        .grid-1 { display:grid; gap:.9rem; }
        label { display:block; margin:0 0 .4rem; font-weight:600; color:#334155; }
        input { width:100%; padding:.9rem 1rem; border:2px solid #e2e8f0; border-radius:12px; font-size:1rem; }
        input:focus { outline:none; border-color: var(--primary); box-shadow: 0 0 0 3px var(--ring); }
        .submit { width:100%; padding:1rem; border:none; border-radius:999px; background:var(--primary); color:#fff; font-weight:800; cursor:pointer; box-shadow: 0 10px 18px rgba(139,92,246,.35); }
        .submit:hover { background:var(--primary-strong); }
        .hint { text-align:center; color:#64748b; margin-top:.9rem; }
        .hint a { color:var(--primary); font-weight:700; text-decoration:none; }
    </style>
</head>
<body>
    <div class="modal">
        <div class="card">
            <h1 class="title">Créer un compte</h1>
            <div class="subtitle">Rejoignez la communauté dès aujourd'hui</div>
            <a class="google-btn" href="#" onclick="return false;">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" width="20" height="20"><path fill="#FFC107" d="M43.611,20.083H42V20H24v8h11.303C33.564,32.91,29.171,36,24,36c-6.627,0-12-5.373-12-12   s5.373-12,12-12c3.059,0,5.842,1.153,7.961,3.039l5.657-5.657C33.64,6.053,28.999,4,24,4C12.955,4,4,12.955,4,24s8.955,20,20,20   s20-8.955,20-20C44,22.659,43.862,21.35,43.611,20.083z"/><path fill="#FF3D00" d="M6.306,14.691l6.571,4.819C14.655,16.108,18.961,13,24,13c3.059,0,5.842,1.153,7.961,3.039l5.657-5.657   C33.64,6.053,28.999,4,24,4C16.318,4,9.656,8.337,6.306,14.691z"/><path fill="#4CAF50" d="M24,44c4.99,0,9.585-1.917,13.058-5.042l-6.028-4.978C29.084,35.463,26.715,36,24,36   c-5.137,0-9.476-3.256-11.289-7.792l-6.493,5.005C9.517,39.556,16.227,44,24,44z"/><path fill="#1976D2" d="M43.611,20.083H42V20H24v8h11.303c-1.131,3.237-3.777,5.768-7.028,6.854   c0.001-0.001,0.002-0.001,0.003-0.001l6.028,4.978C33.955,40.083,44,34,44,24C44,22.659,43.862,21.35,43.611,20.083z"/></svg>
                Utiliser un compte Google
            </a>
            <div class="divider">ou</div>

            {{ form_start(form, { 'attr': { 'novalidate': 'novalidate' } }) }}
                <div class="grid">
                    <div>
                        {{ form_label(form.firstName) }}
                        {{ form_widget(form.firstName, { 'attr': { 'placeholder': 'Votre prénom' } }) }}
                    </div>
                    <div>
                        {{ form_label(form.lastName) }}
                        {{ form_widget(form.lastName, { 'attr': { 'placeholder': 'Votre nom' } }) }}
                    </div>
                </div>
                <div class="grid-1" style="margin-top:.9rem;">
                    <div>
                        {{ form_label(form.email) }}
                        {{ form_widget(form.email, { 'attr': { 'placeholder': 'email@exemple.com' } }) }}
                    </div>
                    <div>
                        {{ form_label(form.plainPassword) }}
                        {{ form_widget(form.plainPassword, { 'attr': { 'placeholder': 'Mot de passe (min. 8 caractères)' } }) }}
                    </div>
                </div>
                <div style="margin-top:1rem;">
                    <button type="submit" class="submit">S'inscrire</button>
                </div>
            {{ form_end(form) }}

            <div class="hint">Déjà un compte ? <a href="{{ path('app_login') }}">Se connecter</a></div>
        </div>
    </div>
</body>
</html> 